---
const { offre } = Astro.props;
---

<div class="bg-white rounded-lg shadow-md overflow-hidden">
  <div class="p-4">
    <h2 class="text-xl font-semibold mb-2">{offre.nomMaison}</h2>
    <p class="text-gray-600 mb-1">{offre.adresse}</p>
    <p class="text-gray-800 font-bold mb-2">{offre.prix} € / mois</p>
    <div class="flex justify-between text-sm text-gray-600">
      <span>{offre.nbChambres} chambres</span>
      <span>{offre.nbSdb} salles de bain</span>
      <span>{offre.surface} m²</span>
    </div>
  </div>
</div>